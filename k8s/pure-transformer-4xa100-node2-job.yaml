---
# Pure Transformer 4x A100 (Concurrent Job - Node 2)
# Target: Resume training from last checkpoint on gpn-fiona-mizzou-1 (4 free A100s)
# Concurrent with existing job on gpn-fiona-mizzou-5
#
apiVersion: batch/v1
kind: Job
metadata:
  name: pure-transformer-4xa100-node2
  namespace: ucsdfutures
  labels:
    app: pure-transformer
    training-type: pretrain
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 3
  ttlSecondsAfterFinished: 86400
  template:
    metadata:
      labels:
        job-name: pure-transformer-4xa100-node2
        app: pure-transformer
    spec:
      restartPolicy: OnFailure
      
      # Target the available A100 node (gpn-fiona-mizzou-1)
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - gpn-fiona-mizzou-1.rnet.missouri.edu
      
      volumes:
      - name: checkpoints
        persistentVolumeClaim:
          claimName: pure-transformer-checkpoints
      - name: hf-cache
        persistentVolumeClaim:
          claimName: pure-transformer-hf-cache
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi
      - name: secret-volume
        secret:
          secretName: hybrid-llm-secrets
      
      containers:
      - name: trainer
        image: nvcr.io/nvidia/pytorch:24.02-py3
        imagePullPolicy: IfNotPresent
        
        env:
        - name: MASTER_ADDR
          value: "localhost"
        - name: MASTER_PORT
          value: "29500"
        - name: WORLD_SIZE
          value: "1"
        - name: RANK
          value: "0"
        
        - name: HF_HOME
          value: /hf-cache
        - name: HF_DATASETS_CACHE
          value: /hf-cache/datasets
        - name: TRANSFORMERS_CACHE
          value: /hf-cache/transformers
        - name: HUGGINGFACE_HUB_CACHE
          value: /hf-cache/hub
        
        - name: WANDB_API_KEY
          valueFrom:
            secretKeyRef:
              name: hybrid-llm-secrets
              key: wandb-api-key
        - name: WANDB_PROJECT
          value: "pure-transformer"
        - name: WANDB_NAME
          value: "xlarge-4xa100-35B-node2-resume"
        - name: WANDB_DIR
          value: /checkpoints/wandb
        
        - name: CUDA_DEVICE_MAX_CONNECTIONS
          value: "1"
        - name: PYTORCH_CUDA_ALLOC_CONF
          value: "max_split_size_mb:512"
        
        - name: NCCL_DEBUG
          value: "WARN"
        - name: NCCL_IB_DISABLE
          value: "0"
        - name: NCCL_P2P_LEVEL
          value: "NVL"
        - name: NCCL_SHM_DISABLE
          value: "0"
        - name: NCCL_SOCKET_IFNAME
          value: "eth0"
        - name: NCCL_ASYNC_ERROR_HANDLING
          value: "1"
        - name: TORCH_NCCL_HEARTBEAT_TIMEOUT_SEC
          value: "1800"
        - name: TORCH_NCCL_ENABLE_MONITORING
          value: "1"
        
        - name: OMP_NUM_THREADS
          value: "12"
        - name: TOKENIZERS_PARALLELISM
          value: "true"
        - name: PYTHONUNBUFFERED
          value: "1"
        
        workingDir: /workspace
        
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -ex
          
          echo "=== CONCURRENT A100 TRAINING (NODE 2) ==="
          nvidia-smi
          
          cd /workspace
          if [ ! -d "NLP" ]; then
            git clone --depth 1 --branch pure-transformer https://github.com/amauro-nexstream-2020/NLP.git
          else
             cd NLP && git fetch origin pure-transformer && git checkout pure-transformer && git reset --hard origin/pure-transformer && cd ..
          fi
          cd NLP
          
          pip install --no-cache-dir --upgrade pip
          pip install --no-cache-dir --upgrade "typing_extensions>=4.0.0" "lightning>=2.6.0" "transformers>=4.57.0" "datasets>=4.4.0" "tokenizers>=0.22.0" "wandb>=0.23.0" "accelerate>=1.12.0" "numpy<2" "tensorboard>=2.15.0" "tqdm>=4.66.0" "huggingface-hub>=0.20.0" "psutil>=5.9.0"
          pip install --no-cache-dir flash-attn --no-build-isolation
          
          mkdir -p /checkpoints/wandb
          chmod -R 777 /checkpoints || true
          
          LOGFILE=/checkpoints/training_4xa100_node2_$(date +%Y%m%d_%H%M%S).log
          
          # Modifications:
          # - Workers: 2 (reduced from 4)
          # - Resume from last checkpoint
          # - Devices: 4
          
          PYTHONPATH=$(pwd) python -m pure_transformer.train_multigpu \
            --model xlarge \
            --devices 4 \
            --nodes 1 \
            --strategy ddp \
            --precision bf16-mixed \
            --total-tokens 35000000000 \
            --global-batch-size 2097152 \
            --micro-batch-size 32 \
            --seq-length 2048 \
            --learning-rate 3e-4 \
            --min-lr 3e-5 \
            --weight-decay 0.1 \
            --warmup-tokens 350000000 \
            --num-workers 2 \
            --checkpoint-dir /checkpoints \
            --resume-from /checkpoints/last.ckpt \
            --save-every-n-steps 500 \
            --log-every-n-steps 10 \
            --use-wandb \
            --wandb-project pure-transformer \
            --fineweb-subset sample-100BT \
            --fineweb-prob 0.65 \
            --finepdf-prob 0.34 \
            --usmle-prob 0.01 \
            --compile \
            --seed 42 \
            2>&1 | tee ${LOGFILE}
          
          exit ${PIPESTATUS[0]}
        
        resources:
          requests:
            memory: "320Gi"
            cpu: "32"
            nvidia.com/a100: "4"
          limits:
            memory: "380Gi"
            cpu: "48"
            nvidia.com/a100: "4"
        
        volumeMounts:
        - name: checkpoints
          mountPath: /checkpoints
        - name: hf-cache
          mountPath: /hf-cache
        - name: dshm
          mountPath: /dev/shm
        - name: secret-volume
          mountPath: /etc/secrets
          readOnly: true
      
      tolerations:
      - key: nvidia.com/gpu
        operator: Exists
        effect: NoSchedule
      - key: nautilus.io/hardware
        operator: Exists
        effect: NoSchedule
      - key: nautilus.io/issue
        operator: Exists
        effect: NoSchedule
      - key: nautilus.io/reservation
        operator: Equal
        value: mizzou
        effect: NoSchedule
